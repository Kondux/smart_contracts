[pytest]
markers =
    integration: marks tests as integration tests (requires running Anvil fork)
    slow: marks tests as slow running
testpaths = scripts
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning:websockets.*
